main
│
├── feat/<feature>
├── fix/<bug>
├── refactor/<area>
├── test/<area>
├── docs/<topic>
└── chore/<task>

Day One Branch

The first development branch for the Restaurant POS project is:

feat/domain-foundation

Create it with:

git checkout main
git pull origin main
git checkout -b feat/domain-foundation

Push it to GitHub:

git push -u origin feat/domain-foundation
